PRAGMA foreign_keys = 1;

-- offers table
CREATE TABLE offers (
    offer BLOB NOT NULL PRIMARY KEY
);

-- offers queries
listOffers:
SELECT * FROM offers;

insertOffer:
INSERT INTO offers VALUES (?);